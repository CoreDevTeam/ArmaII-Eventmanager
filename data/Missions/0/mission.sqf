// Mission 0// IED beseitigen// Timer :// Mission startenmission_complete = false;publicVariable "mission_complete";_local_mission_complete = false;_local_mission_failed = false;fnc_timer = compile preprocessFile "data\Missions\timer.sqf";timeup = false;publicVariable "timeup";// create Timer[2600] call fnc_timer;// Setup Mission stuff// Task aufstellen// Zufall der Location auswählen. Siehe debug\Locations.txt// Position der Location = Pos der IED vielleciht x und y der Pos_arr verändern ? +10/-10// IED dort erstellen// Start Mission Loopwhile ( !timeup && _local_mission_complete ) do {	// Check ob IED als Vehicle noch alive ist	// wenn nicht dann _local_missions_complete = true	// wenn timeup == true dann wird loop abgebrochen	// loop wird abgebrochen};//nochmals Timeup checken// wenn false, dann mit mission_complete fortfahren// wenn true, dann _local_mission_failed = true;if ( !timeup ) then { _local_mission_complete = true; _local_mission_failed = false; }; if ( timeup  ) then { _local_mission_failed = true; _local_mission_complete = false; }; //Task Beenden je nach Erfolg (_local_mission_complete == true )/Fail (_local_mission_failed == true)if ( _local_mission_complete ) then {};if ( _local_mission_failed ) then {};sleep 10;// Mission stoppen/neue Mission startenmission_complete = true;publicVariable "mission_complete";